# atuin — local-only history (no sync)
auto_sync = false
update_check = false
sync_address = ""

style = "compact"
inline_height = 16
invert = false

search_mode = "fuzzy"
filter_mode = "global"
filter_mode_shell_up_key_binding = "session"

enter_accept = true
